spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d71vfgnpm1nc73f6aipg-a:5432/complaintBox?sslmode=require}
spring.datasource.username=${DB_USERNAME:collegecomplaintbox_user}
spring.datasource.password=${DB_PASSWORD:aHOeSWiwkXQgxyltIhy9X0VESGZTh9ff}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# Server Configuration
server.port=${PORT:8080}
server.servlet.context-path=/
spring.application.name=college-complaint-system

# JWT Configuration
app.jwtSecret=mySecretKeyForJWTTokenGenerationAndValidationWithMinimum32Characters
app.jwtExpirationInMs=86400000

# Logging Configuration
logging.level.root=INFO
logging.level.com.example.demo=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
